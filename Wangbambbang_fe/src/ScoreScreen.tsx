import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

const ScoreScreen = () => {
    return (
        <View>
            <Text>ScoreScreen</Text>
        </View>
    );
}

export default ScoreScreen;